# 參與貢獻

歡迎提交翻譯修正、遺漏回報和 Omarchy 新版本相容修補程式。

## 翻譯原則

- 使用台灣繁體中文（zh-TW），優先採用現代台灣 Linux 桌面慣例（GNOME／KDE／Windows zh-TW 譯法）。
- 譯名沒有定論時，以台灣微軟的在地化用語為優先參考；`support.microsoft.com` 部分頁面是機器翻譯，引用前請確認是人工在地化內容。
- 詞彙以 [docs/glossary.md](docs/glossary.md) 為準；新增詞彙時請查照 IICM 電腦名詞譯名表，並在詞彙表記錄採用譯名與偏離理由。
- Omarchy、Hyprland、應用程式名稱、協定名稱和程式碼識別字保持原文。
- 不翻譯真實檔案路徑、指令引數、鍵名和使用者輸入內容。
- 第三方應用程式通知保持原文，只翻譯 `omarchy-action` 發出的已知系統通知。
- 動態值應分別翻譯。例如版面配置內部值 `scrolling` 顯示為「捲動版面配置」，但實際設定仍保留 `scrolling`。

## 標點原則

- 使用全形標點（，。！？：；、）。
- 顯示用引號一律使用「」與巢狀『』；全形彎引號（U+2018、U+2019、U+201C、U+201D）不在使用範圍，程式碼語法中的 ASCII 引號不受此限。
- 系統介面應盡量避免多餘引號，能改寫語句時就不要加引號。

## 提交前檢查

```bash
./tests/smoke.sh
```

`tests/terminology.sh` 會檢查殘留簡體字、大陸用語與禁用標點；新增字串若違規會直接失敗。

請勿提交以下內容：

- `~/.config/omarchy/plugins/` 中產生的 Omarchy 外掛副本
- 通知歷程、日誌、截圖或包含個人資訊的設定
- `/usr/share/omarchy` 的完整原始碼副本
- API 金鑰、權杖、網路密碼或位置歷程

## 回報相容問題

請附上：

- `omarchy version` 輸出
- 未繁體化文字及其所在面板
- `omarchy-zh-tw-sync` 的錯誤輸出
- 是否安裝過同名自訂外掛

截圖前請遮蔽帳號、通知、路徑和其他個人資訊。
